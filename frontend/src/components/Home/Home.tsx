import React from 'react';
import Sidebar from '../Sidebar/Sidebar';

const Home:React.FC = () => {
	console.log();

	return (
		<main className="home">
			<Sidebar />
		</main>
	);
};

export default Home;
